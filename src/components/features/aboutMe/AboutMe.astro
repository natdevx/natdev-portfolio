<!-- Contenedor principal -->
<section
  id="sobre-mi"
  class="container w-full max-w-6xl mx-auto px-4 md:px-6 lg:px-8 flex flex-col md:flex-row items-center justify-between gap-12 sobre-mi opacity-0 translate-y-10 transition-all duration-1000 ease-out"
>
  <!-- Contenido -->
  <div class="w-full flex flex-col p-4">
    <h2 class="font-bold text-5xl text-left mb-6 text-[var(--color-heading)]">
      Sobre mí
    </h2>

    <!-- Tarjeta principal -->
    <article
      class="group relative w-full bg-[var(--color-surface)] shadow-md rounded-2xl overflow-hidden 
             transition-all duration-500 ease-in-out transform hover:-translate-y-2 hover:shadow-2xl">
      <img
        src="/about-me.webp"
        alt="Imagen representativa sobre mí"
        class="w-full h-[400px] object-cover brightness-90 group-hover:brightness-100 transition-all duration-500"
        loading="lazy"
      />

      <div class="p-6">
        <h3 class="text-2xl font-semibold mb-2 text-[var(--color-primary)]">
          Ingeniera en Sistemas Computacionales · Desarrolladora Web
        </h3>
        <p class="text-[var(--color-secondary)] font-medium mb-3">
          Apasionada por el diseño moderno y la lógica del código.
        </p>
        <p class="text-base leading-relaxed text-gray-500">
          Soy Ingeniera en Sistemas Computacionales apasionada por el diseño y desarrollo web. 
          Me especializo en la parte frontend, trabajando con HTML, CSS, JavaScript y Angular, y actualmente aprendo React. 
          También tengo experiencia en backend con Laravel, Node.js y bases de datos. 
          Me encanta aprender nuevas tecnologías y transformar ideas en proyectos funcionales y visualmente atractivos.
        </p>
      </div>
    </article>
  </div>
</section>

<!-- Script de animación al hacer scroll -->
<script>
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("opacity-100", "translate-y-0");
          entry.target.classList.remove("opacity-0", "translate-y-10");
          observer.unobserve(entry.target); // Evita que se repita
        }
      });
    },
    { threshold: 0.2 }
  );

  document.querySelectorAll(".sobre-mi").forEach((el) => observer.observe(el));
</script>

<!-- Estilos opcionales -->
<style>
  /* Ajuste de la tarjeta en pantallas pequeñas */
  @media (max-width: 768px) {
    .sobre-mi img {
      height: 280px;
    }
  }

  /* Pequeña animación de opacidad al hacer hover en la imagen */
  .group:hover img {
    filter: brightness(1);
  }
</style>
